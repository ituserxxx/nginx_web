# 2026-09-02

## README.md 全面更新（对齐登录鉴权 + 单文件构建后的现状）

### 背景
README 此前只到「站点配置」为止，缺登录鉴权、单文件构建、`APP_PASSWORD` 等内容，
接口清单也漏了 login/me/logout 三个端点。

### 改动要点
- **顶部**：重写简介（Nginx 可视化控制台）+ 四行特性概览（Go 零依赖 / Vue3+antdv / build.sh 单文件 / 登录鉴权），
  并加提示：运维能力仅在 Linux 生效，非 Linux 接口返回 `supported:false`。
- **新增「项目结构」章节**：树状列出 start.sh / build.sh / .env.example / server/(main.go, main_test.go,
  auth_test.go, embed.go, noembed.go) / web/src/App.vue；说明前端无路由、用 currentMenu 切视图。
- **本地开发**：前端访问处补「先进入登录页，默认密码 admin」并链到登录鉴权章节。
- **环境变量表**：新增 `APP_PASSWORD`（默认 admin，环境变量优先级高于 .env）。
- **部署章节**：补充 `.env` 必须与后端同工作目录，否则**静默回退为默认 admin**（易踩的坑）；
  并推荐改用单文件构建。
- **接口章节**：拆成「公开接口」与「业务接口（需登录，未登录 401）」两组，补 login/me/logout，
  并把 sites/create 的完整字段列出（domain/listen/root/proxyPort/proxyScheme/proxyHost/ssl/cert/key）。

### 核对过的代码事实（避免文档漂移）
- `configureArgs()`：prefix + ssl + v2 + gzip_static + `--with-cc-opt=-Wno-error=deprecated-declarations`。
- 安装 9 步顺序与 README 一致：准备源码目录 → 装依赖 → 下载 → 解压(先清理) → configure →
  make -jN → make install → 校验+启动+软链 → config.d include + reload。
- `siteInput` 字段名与文档一致（ProxyScheme/ProxyHost/ProxyPort/SSL/Cert/Key）。
- start.sh 环境变量 PORT/VITE_PORT/VITE_POLL/SKIP_PORT_CLEAN/NO_COLOR 与文档表格一致。

### 遗留
- 本会话 sandbox 的 safe-delete 拦截 `rm`，`server/dist`（embed 验证残留）与
  `server/tmp/nginx_web.test` 未能删除；两者均已被 .gitignore 忽略，不影响提交。
  用户在 WSL 下 `rm -rf server/dist server/tmp` 即可。
